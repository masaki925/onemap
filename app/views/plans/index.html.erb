<h1>Listing plans</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Start datetime</th>
    <th>End datetime</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @plans.each do |plan| %>
  <tr>
    <td><%= link_to plan.title, plan %></td>
    <td><%= plan.start_datetime %></td>
    <td><%= plan.end_datetime %></td>
    <td><%= link_to 'Edit', edit_plan_path(plan) %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Plan', new_plan_path %>
